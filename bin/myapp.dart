import 'bublesort.dart';

void main(List<String> arguments) {
  // List<int> list = [23, 4, 34, 32, 42, 5, 8, 1];
  // int incValue = 0;
  // Sort sort = Sort(list, incValue);
  // sort.bubbleSort(); // do bubble sorting
 
}
